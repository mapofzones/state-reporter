checker:
  host: http://<ip>:<port>
  sync-time: ${SYNC_TYME} # 14400000 # in milliseconds -> 1000 * 60 * 60 * 4 = 14 400 000 = 4 hour

spring:
  main:
    web-application-type: none

    jpa:
      open-in-view: false

      hibernate:
        ddl-auto: validate

      properties:
        hibernate:
          schema_update:
            unique_constraint_strategy: RECREATE_QUIETLY
          jdbc:
            lob:
              non_contextual_creation: true

